package com.luban.netty.bytebuf;

import io.netty.buffer.*;
import io.netty.util.CharsetUtil;
import io.netty.util.internal.PlatformDependent;
import sun.misc.Signal;
import sun.misc.SignalHandler;

import java.nio.ByteBuffer;
import java.nio.charset.Charset;
import java.util.concurrent.atomic.AtomicIntegerFieldUpdater;

public class ByteBufferTest {

    private volatile int age=32;
    public static void main(String[] args) {
//        ByteBufferTest test = new ByteBufferTest();
//        AtomicIntegerFieldUpdater<ByteBufferTest> AIF_UPDATER
//                = AtomicIntegerFieldUpdater.newUpdater(ByteBufferTest.class, "age");

//        ByteBuf buf = Unpooled.buffer(10);//非池化10字节大小的ByteBuf
//        ByteBuffer buffer = ByteBuffer.allocate(88);
//
//
//        String value = "Netty权威指南";
//        System.out.println(value.getBytes().length);
//        buffer.put(value.getBytes());
//        System.out.println(buffer);
//        buffer.flip();
//        System.out.println(buffer.getInt());
//        buffer.compact();
//        buffer.flip();
//        System.out.println(buffer);
//        System.out.println(buffer.getInt());




    }




}
